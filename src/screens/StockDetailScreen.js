import React, { useState, useEffect } from 'react';
import type {Node} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  TouchableOpacity,
  useColorScheme,
  View,
  Image
} from 'react-native';

const StockDetailScreen: () => Node = () => {
    return(
        <SafeAreaView>

        </SafeAreaView>
    )
}

export default StockDetailScreen;
